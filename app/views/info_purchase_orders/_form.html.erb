<%= form_with(model: info_purchase_order, local: true) do |form| %>
  <% if info_purchase_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(info_purchase_order.errors.count, "error") %> prohibited this info_purchase_order from being saved:</h2>

      <ul>
      <% info_purchase_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class='container'>
    <div class="field-control">
      <%= form.label :provider_id %><br>
      <%= form.select :provider_id, Provider.all.pluck(:name, :id) %>
    </div>

    <div class="field">
      <%= form.label :user_id %><br>
      <%= form.select :user_id, User.all.pluck(:email, :id) %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
</div>
